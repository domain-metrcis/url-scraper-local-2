{
  "url": "https://claude.ai",
  "variables": {
    "message": "tell me about pm modi",
    "query_id": ""
  },
  "options": {
    "new_tab": true,
    "close_tab": true,
    "close_tab_on_error": true
  },
  "actions": [
    {
      "type": "delay",
      "timeout": 3000
    },
    {
      "type": "wait_for",
      "selectors": [
        "div[contenteditable=\"true\"][data-testid=\"chat-input\"]",
        "div[contenteditable=\"true\"]",
        "div[data-testid=\"chat-input\"]"
      ],
      "timeout": 15000
    },
    {
      "type": "evaluate",
      "script": "var el = document.querySelector('div[contenteditable=\"true\"][data-testid=\"chat-input\"]') || document.querySelector('div[contenteditable=\"true\"]') || document.querySelector('div[data-testid=\"chat-input\"]'); if (!el) throw new Error('chat input not found'); el.focus(); await new Promise(r => setTimeout(r, 300)); var sel = window.getSelection(); var range = document.createRange(); range.selectNodeContents(el); sel.removeAllRanges(); sel.addRange(range); document.execCommand('delete', false, null); await new Promise(r => setTimeout(r, 200)); document.execCommand('insertText', false, '${message}'); await new Promise(r => setTimeout(r, 1000)); var btn = document.querySelector('button[aria-label=\"Send message\"]') || document.querySelector('button.Button_claude__c_hZy') || document.querySelector('button[type=\"submit\"]'); if (!btn) throw new Error('send button not found'); btn.click(); return 'message_sent';",
      "set_variable": "send_status"
    },
    {
      "type": "evaluate",
      "script": "await new Promise(r => setTimeout(r, 10000)); var prev = ''; var stable = 0; var minLength = 100; for (var i = 0; i < 90; i++) { await new Promise(r => setTimeout(r, 2000)); var els = document.querySelectorAll('div.font-claude-response'); if (els.length === 0) { els = document.querySelectorAll('div.standard-markdown'); } if (els.length === 0) { els = document.querySelectorAll('div[data-is-streaming=\"false\"] div.font-claude-response'); } if (els.length === 0) { els = document.querySelectorAll('div[data-is-streaming=\"false\"]'); } if (els.length === 0) { els = document.querySelectorAll('div.text-base.gap-x-2'); } if (els.length > 0) { var txt = els[els.length - 1].innerText || ''; if (txt.length > minLength && txt === prev) { stable++; if (stable >= 3) { return txt; } } else { if (txt !== prev) { stable = 0; } prev = txt; } } } return prev || '';",
      "set_variable": "claude_response"
    },
    {
      "type": "set_variable",
      "name": "success",
      "value": "true"
    }
  ],
  "webhook": "",
  "webhook_payload": {
    "query_id": "${query_id}",
    "success": "${success}",
    "message": "${message}",
    "response": "${claude_response}",
    "error_message": "${error_message}",
    "service_type": "claude"
  }
}
