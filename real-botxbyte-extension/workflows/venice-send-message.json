{
  "url": "https://venice.ai/chat",
  "variables": {
    "message": "tell me about pm modi",
    "query_id": ""
  },
  "options": {
    "new_tab": true,
    "close_tab": true,
    "close_tab_on_error": true
  },
  "actions": [
    {
      "type": "delay",
      "timeout": 3000
    },
    {
      "type": "wait_for",
      "selectors": [
        "textarea[placeholder=\"Ask anything privately...\"]",
        "textarea[placeholder*=\"Ask anything\"]",
        "textarea.chakra-textarea",
        "div.cm-content",
        "div[contenteditable=\"true\"]",
        "textarea"
      ],
      "timeout": 15000
    },
    {
      "type": "evaluate",
      "script": "var el = document.querySelector('textarea[placeholder=\"Ask anything privately...\"]') || document.querySelector('textarea[placeholder*=\"Ask anything\"]') || document.querySelector('textarea.chakra-textarea') || document.querySelector('div.cm-content') || document.querySelector('div[contenteditable=\"true\"]') || document.querySelector('textarea'); if (!el) throw new Error('prompt input not found'); var isContentEditable = el.tagName.toLowerCase() === 'div' || el.getAttribute('contenteditable') === 'true'; if (isContentEditable) { el.focus(); await new Promise(r => setTimeout(r, 300)); var sel = window.getSelection(); var range = document.createRange(); range.selectNodeContents(el); sel.removeAllRanges(); sel.addRange(range); document.execCommand('delete', false, null); await new Promise(r => setTimeout(r, 200)); document.execCommand('insertText', false, '${message}'); } else { el.focus(); await new Promise(r => setTimeout(r, 300)); el.value = ''; el.dispatchEvent(new Event('input', { bubbles: true })); await new Promise(r => setTimeout(r, 200)); var nativeInputValueSetter = Object.getOwnPropertyDescriptor(window.HTMLTextAreaElement.prototype, 'value').set; nativeInputValueSetter.call(el, '${message}'); el.dispatchEvent(new Event('input', { bubbles: true })); } await new Promise(r => setTimeout(r, 1000)); var btn = document.querySelector('button[type=\"submit\"][aria-label=\"Submit chat\"]') || document.querySelector('button[aria-label=\"Submit chat\"]') || document.querySelector('button[type=\"submit\"]') || document.querySelector('button[aria-label*=\"Submit\" i]') || document.querySelector('button[aria-label*=\"Send\" i]'); if (!btn) throw new Error('send button not found'); btn.click(); return 'message_sent';",
      "set_variable": "send_status"
    },
    {
      "type": "evaluate",
      "script": "await new Promise(r => setTimeout(r, 5000)); var prev = ''; var stable = 0; for (var i = 0; i < 60; i++) { await new Promise(r => setTimeout(r, 2000)); var els = document.querySelectorAll('div.chakra-text.css-1e6z99x'); if (els.length === 0) { els = document.querySelectorAll('div[class*=\"message\"]'); } if (els.length === 0) { els = document.querySelectorAll('div[class*=\"markdown\"]'); } if (els.length === 0) { els = document.querySelectorAll('div[class*=\"response\"]'); } if (els.length === 0) { els = document.querySelectorAll('div[class*=\"prose\"]'); } if (els.length > 0) { var txt = els[els.length - 1].innerText || ''; if (txt.length > 0 && txt === prev) { stable++; if (stable >= 2) { return txt; } } else { stable = 0; prev = txt; } } } return prev || '';",
      "set_variable": "venice_response"
    },
    {
      "type": "set_variable",
      "name": "success",
      "value": "true"
    }
  ],
  "webhook": "",
  "webhook_payload": {
    "query_id": "${query_id}",
    "success": "${success}",
    "message": "${message}",
    "response": "${venice_response}",
    "error_message": "${error_message}",
    "service_type": "venice"
  }
}
